# ── Web Framework ─────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1

# ── HTTP & Parsing ────────────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2

# ── Network / OSINT ───────────────────────────────────────────────────
dnspython==2.6.1
python-whois==0.9.4

# ── External Security Tools ───────────────────────────────────────────
# python-nmap: Python wrapper cho Nmap (dung boi ReconAgent Phase 1)
# Yeu cau: Nmap binary phai duoc cai rieng:
#   Windows: winget install -e --id Insecure.Nmap
#   Linux:   sudo apt install nmap
# ReconAgent se fallback sang socket scanning neu khong co Nmap.
python-nmap>=0.7.1

# python-owasp-zap-v2.4: Python client cho OWASP ZAP REST API
# Dung boi ScannerAgent (Phase 2a) de lay ZAP alerts.
# Yeu cau: ZAP daemon phai chay rieng (xem HUONG_DAN_CHAY.md).
# ScannerAgent se fallback sang ZAP REST API truc tiep neu package nay chua cai,
# va fallback sang custom scanner neu ZAP daemon chua chay.
python-owasp-zap-v2.4>=0.0.21

# ── Templating & Report ───────────────────────────────────────────────
jinja2==3.1.4

# ── PDF Export (Linux only, requires GTK libraries) ──────────────────
# WeasyPrint: can cai dat libpango, libcairo tren Linux
# Windows: bo qua, chi xuat HTML
weasyprint>=60.0; sys_platform != 'win32'

# ── LLM (DeepSeek executive summary, CrewAI orchestration) ───────────
anthropic>=0.30.0

# ── CrewAI — Multi-Agent Orchestration ───────────────────────────────
# crewai ke ca phu thuoc: langchain, pydantic v2, openai, litellm...
crewai>=0.85.0

# litellm: can thiet de CrewAI ket noi DeepSeek va cac provider non-native
litellm>=1.50.0

# ── LangChain OpenAI adapter (dung cho DeepSeek / OpenAI LLM config) ─
langchain-openai>=0.2.0
